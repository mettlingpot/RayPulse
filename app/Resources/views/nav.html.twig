 <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('mp_platform_home') }}">RayPulse</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li class="<?= set_active('index') ?>"><a href="{{ path('mp_platform_home') }}">Accueil</a></li>
            <li class="<?= set_active('inscription') ?>"><a href="{{ path('mp_user_register') }}">Inscription</a></li>
            <li class="<?= set_active('login') ?> ">        
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a href="{{ path('logout') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('login') }}">Connexion</a>
                {% endif %}
            </li>
            <li> {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}<a href="{{ path('mp_user_profil') }}">Gérer les événements de {{ app.user.username }}{% endif %}</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>